# Heap
>
A heap is a [[tree|tree-like]] data structure that allows for the insertion of objects in a pseudo-sorted manner.

Most information on heaps are intertwined with [[priority queue]]. A heap is just a simplified priority queue. 