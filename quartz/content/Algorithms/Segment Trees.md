# Segment trees
#tofinish

>
A segment tree is a [[tree|tree-like]] data structure which helps to quickly acquire a [[range query]] in O(logN) time. It is more generalized, but a bit more complex, and memory intensive. 
