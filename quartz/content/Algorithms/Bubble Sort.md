# Bubble Sort
---
>[!summary] A simple [[sorting]] algorithm involving *swapping* neighbouring elements in a list depending on which element is greater, until the list is fully sorted. The time complexity of this algorithm is $\pmb{\mathcal{O}(N)}$.
